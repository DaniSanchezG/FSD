'use strict';

// Condicional IF
// Si A es igual a B, entonces haz algo

// Operadores relacionales

// > mayor que
// < menor que
// >= mayor o igual que
// <= menor o igual que
// == igual a
// != no igual a
// === igual y del mismo tipo
// !== no igual y del mismo tipo

// Operadores logicos

// && AND   y
// || OR    o
// ! NOT   no
// ? : si/no   

var edad1 = 20;
var edad2 = 30;

//if else

if(edad1 > edad2) {
    alert('Edad 1 es mayor que edad 2');
}else{
    alert('La edad 1 es inferior a la edad 2');
}

